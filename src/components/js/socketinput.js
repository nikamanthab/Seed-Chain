let fillingTheInputElements = (str) => {
    // let ele = document.querySelector(`.${element}`);
    // ele.textContent = str;
    this.setState({textcontent:"nitin"});
}
export default fillingTheInputElements;